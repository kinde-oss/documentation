---
page_id: d5ab49ac-bf16-487a-b1a1-e07f68e42be7
title: Passwordless authentication
sidebar:
  order: 4
relatedArticles:
  - 43e8aa2d-76a4-4445-ae90-84d3f1a55fcb
  - 0145d1ce-564e-4c28-820f-2f126abbfe3a
  - 8a385f58-5e01-437c-8128-6b86ee8ff382
description: "Authenticate with emailed or SMS one-time codes, and add a Sign in with a code option to the password page"
featured: false
deprecated: false
topics:
  - authenticate
  - authentication-methods
  - passwordless
sdk: []
languages: []
audience:
  - developer
  - product-manager
complexity: beginner
keywords:
  - OTP
  - one-time passcode
  - email code
  - phone code
  - username + code
  - sign in with a code
ai_summary: "Guide to passwordless authentication in Kinde using one-time passcodes (OTP) instead of user-managed passwords. Explains why Kinde uses OTPs rather than magic links, that codes can be sent by email or phone, that they expire after two hours, and that they are also used in multi-factor authentication. Covers setup for Email connections Code, Phone connections Code, and Username connections Code, including Twilio and paid-plan requirements for phone. Describes offering a Sign in with a code button to password users on Email plus password or Username connections without switching the app fully to passwordless. Includes troubleshooting if a user does not receive a code, including verified-email checks for the password-page flow, and links to attack protection settings. Intended for developers and product managers."
updated: 2026-08-22
---

Passwordless authentication is a type of authentication that does not require end-users to set or maintain passwords for access to an application. Instead, they authenticate using a one-time passcode (OTP).

<Aside>

The email + passwordless method is switched on by default in all new Kinde businesses

</Aside>

## About one-time passcodes (OTPs)

Kinde does not support magic links as a password alternative, instead, we prefer to use one-time passcodes (OTPs) as they are more secure, and require manual entry as opposed to a single click.

For example, someone with access to your email could click a link to get instant access to an application, but they cannot use the code unless they have initiated the correct sign in flow and have your sign-in identity as well. If you receive the OTP via SMS, someone would need to have your device and unlock code, to access it.

A OTP can be issued via email or phone, depending how you have set up authentication. It is also common to use OTPs as a factor in [multi-factor authentication](/authenticate/multi-factor-auth/about-multi-factor-authentication/).

Passcodes issued from Kinde expire after 2 hours.

## Set up passwordless authentication

1. In Kinde, go to **Settings > Environment > Authentication**.
2. In the **Passwordless** section, select **Configure** on of the relevant tile:
    - **Email connections > Code**
    - **Phone connections > Code**
    - **Username connections > Code**
3. Select which applications will use this authentication method.
4. Select **Save**.

   <Aside title="About phone authentication">

   Phone authentication requires that you have a [Twilio](https://www.twilio.com/en-us) account. You need to enter your Twilio account details and [upgrade to Kinde Pro](https://kinde.com/pricing/) if you want your users to authenticate using phone + code. [Learn more](/authenticate/authentication-methods/phone-authentication/).

   </Aside>

## Offer a code to password users

You do not have to choose between passwords and passcodes for every user. If your app uses **Email + password** or **Username** authentication, you can add a **Sign in with a code** button to the password page. Users who select it get a one-time passcode by email and sign in without their password, while everyone else continues to use their password as normal.

This is set per connection, in the password connection's configuration. See [Let users sign in with a code instead of a password](/authenticate/authentication-methods/password-authentication/#let-users-sign-in-with-a-code-instead-of-a-password).


## If a user does not receive a code

It should not happen often, but occasionally users do not receive their passcode. Here's a few suggestions.

- Tell the user to check their junk folder - some email providers, systems, and devices have security in place to prevent spam. An OTP from an unknown provider (like Kinde) might get accidentally treated as such.
- Once or twice we have come across a domain provider who has added Kinde to a denylist and OTPs from us get rejected. You'll need to contact us so we can investigate and arrange allowlisting. This is a very rare cause of failed OTPs.
- If the user reached the code screen from the **Sign in with a code** button on the password page, check that they have a verified email address on their profile. Without one, Kinde shows the code screen but does not send a code.

## Attack protection settings

Kinde allows you to control the number of sign-in attempts a user gets, how long they get locked out after a failed sign-in attempt, etc. See [Attack protection](/build/set-up-options/attack-protection/).
