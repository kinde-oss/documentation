---
page_id: 720fcdda-daa6-4dff-ad2d-177af555e6bb
title: Set up authentication methods in Kinde
sidebar:
  order: 1
  label: Set up auth methods
tableOfContents:
  maxHeadingLevel: 3
relatedArticles:
  - 318ccfa9-c7f0-40d9-a2ad-814e4e400af7
  - 98f6868a-7871-4ece-ba9c-2a7b21d1b322
  - a5225946-27ad-41c0-a3c1-9a6d735e3efb
  - c62fc730-1d48-42fe-a05e-34f4d330887d
app_context:
  - m: settings
    s: authentication
  - m: user
    s: roles
  - m: settings
    s: permissions
  - m: user
    s: mfa
  - m: settings
    s: mfa
description: "Configure email, phone, username, social, and enterprise auth per app, plus an emailed code on the password page"
featured: false
deprecated: false
topics:
  - authenticate
  - authentication-methods
  - passwordless
  - enterprise-connections
sdk:
  - kinde-management-api
languages: []
audience:
  - developer
  - product-manager
complexity: beginner
keywords:
  - email authentication
  - phone authentication
  - username authentication
  - social sign-in
  - enterprise connections
  - SAML
  - sign in with a code
ai_summary: "Comprehensive guide to configuring authentication methods in Kinde at the environment level. Covers enabling email passwordless or email password per application, with the constraint that an application cannot use both email connections at once, and notes the Sign in with a code option on the password page as an alternative, linking to the dedicated guide for using password and passwordless auth at the same time. Also covers phone authentication, username plus password, username plus passwordless code, social connections, and enterprise methods such as Microsoft Entra ID and SAML. Includes a pointer to managing social and enterprise connections via the Kinde Management API. Intended for developers and product managers setting up user authentication."
updated: 2026-08-22
---

Kinde supports many authentication options that let you control how users access your applications.

You can set different authentication requirements for different applications and for different organizations (if you use [organizations](/build/organizations/add-and-manage-organizations/)).

Set up all your required authentication methods at the Environment level first.

## Passwordless authentication

### Email + code

A user enters their email address and Kinde sends a one-time passcode (OTP) to that address.

<Aside>
Email + code is enabled by default for all new Kinde businesses.
</Aside>

1. Under the **Passwordless connections** section > **Email + code**, select **Configure**.
2. In the popup that appears, enable the applications that will use this authentication method.
3. Select **Save**.

### Phone

You can allow users to authenticate using their phone number as their sign-in identity.

<Aside>
Kinde includes 10 free SMS messages per month for testing. You need to set up a third-party SMS service to send SMS messages to your users. Learn more about [phone authentication](/authenticate/authentication-methods/phone-authentication/).
</Aside>

1. Under the **Passwordless connections** section > **Phone**, select **Configure**.
2. In the popup that appears, enable the applications that will use this authentication method.
3. Select **Save**.

### Username + code

You can allow users to authenticate using a username as their sign-in identity.

<Aside>
When using the username authentication method, users must still provide an email on sign-up. This provides a way to receive the OTP. Learn more about [username authentication](/authenticate/authentication-methods/username-authentication/).
</Aside>

1. Under the **Passwordless connections** section > **Username + code**, select **Configure**.
2. In the popup that appears, enable the applications that will use this authentication method.
3. Select **Save**.

## Password authentication

### Email + password

<Aside type="warning">
You cannot enable both **Email + code** and **Email + password** authentication for the same app at the same time. Disable one of them before enabling the other.
</Aside>

1. Under the **Password connections** section > **Email + password**, select **Configure**.
2. In the popup that appears, enable the applications that will use this authentication method.
3. Select **Save**.

### Username

Users authenticate with a username and password.

<Aside>
When using the username authentication method, users must still provide an email on sign-up. This provides a way to recover their account if they forget their password. Learn more about [username authentication](/authenticate/authentication-methods/username-authentication/).
</Aside>

1. Under the **Password connections** section > **Username**, select **Configure**.
2. In the popup that appears, enable the applications that will use this authentication method.
3. Select **Save**.

## Social authentication

You can allow users to authenticate using social profiles such as Google, Facebook, LinkedIn, etc.

1. Under the **Social connections** section, select **Add connection**.
2. In the popup that appears, select the social apps you want.
3. Select **Save**.
4. Set up the connection for each social app you selected. 

See the [list of social sign-in providers supported by Kinde](/authenticate/social-sign-in/add-social-sign-in/).

## Enterprise authentication

You can allow users to authenticate using enterprise profiles such as Microsoft Entra ID, Google Workspace, etc.

1. Under the **Enterprise connections** section, select **Add connection**.
2. In the popup that appears, select the enterprise app you want.
3. Select **Save**.
4. Set up the connection for each enterprise app you selected. 

See the [list of enterprise sign-in providers supported by Kinde](/authenticate/enterprise-connections/about-enterprise-connections/).

## Custom authentication

Kinde supports the following custom authentication methods:

### Custom OAuth 2.0

1. Under the **Social connections** section, select **Add connection**.
2. In the popup that appears, select **Custom OAuth 2.0**, then select **Next**.
3. Configure the connection details, then select **Save**.

Read the full guide to setting up [custom OAuth 2.0 connections](/authenticate/custom-configurations/custom-oauth2-connection/).

### Custom SAML

1. Under the **Enterprise connections** section, select **Add connection**.
2. In the popup that appears, select **Custom SAML**, then select **Next**.
3. Configure the connection details, then select **Save**.

Read the full guide to setting up [custom SAML connections](/authenticate/enterprise-connections/custom-saml/).

## Add and manage social and enterprise connections via API

Use [Kinde’s management API](/kinde-apis/management#tag/connections) to manage social and enterprise connections. You can list connections, add new connections, retrieve connection details, and update existing connections.
