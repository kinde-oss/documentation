---
page_id: 0145d1ce-564e-4c28-820f-2f126abbfe3a
title: Password authentication
sidebar:
  order: 3
relatedArticles:
  - 26e55a64-13dd-4c7b-b9ad-e7595903ddc8
  - d5ab49ac-bf16-487a-b1a1-e07f68e42be7
  - 43e8aa2d-76a4-4445-ae90-84d3f1a55fcb
description: "Offer password sign-in with hashed storage, reset flows, and an optional emailed code on the password page"
featured: false
deprecated: false
topics:
  - authenticate
  - authentication-methods
  - password
sdk: []
languages: []
audience:
  - developer
  - product-manager
complexity: beginner
keywords:
  - password strength
  - password reset
  - forgot password
  - temporary password
  - sign in with a code
  - one-time passcode
  - email + password
ai_summary: "Guide to password authentication in Kinde. Explains when passwords are appropriate and recommends stronger options such as one-time passcodes and multi-factor authentication. Covers verified contact identity requirements for resets, hash-encrypted password storage that is never visible to admins or Kinde, and strength rules including an eight-character minimum, blocking of common passwords, lockout after failed attempts, and no complexity requirements. Describes reset options: user-initiated forgot password, admin or API-triggered reset, and issuing a temporary password. Documents the Show Sign in with a code on password page connection setting for Email plus password and Username connections, including where codes are sent, when the button is hidden, enumeration protection when a user has no verified email, that MFA and other flow steps still apply after a code, and how to customize the button and separator text. Includes steps to enable password authentication across apps or for a single application. Intended for developers and product managers."
updated: 2026-08-22
---

Password authentication is where an end user supplies and maintains their own password to access your app or project.

Depending on your authentication needs and security requirements, you might be okay to allow users to authenticate with a password. However, we recommend using a more secure method, such as through one-time-passwords, or by adding [multi-factor authentication](/authenticate/multi-factor-auth/about-multi-factor-authentication/) to the sign in experience.

## Passwords and identity verification

To reset a password for a user, or allow them to reset their own password, they need to have a [verified contact identit](/authenticate/about-auth/identity-and-verification/)y such as an email in their Kinde profile. You’ll need this to securely set a temporary password or to trigger a password reset for a user.

## Password visibility and encryption

The way Kinde is built ensures that user passwords can only be stored as hash-encrypted strings, meaning they are not visible and cannot be deciphered or accessed to be exploited. Neither you in your business or Kinde can see any passwords that a user has set.

## Password strength

Kinde supports the following password requirements:

- 8 character minimum
- Blocking of 1,000,000 most common passwords
- 5 incorrect attempts locks account out for 5 minutes
- No complexity requirements or character limitations - combined with allowing long passwords, this provides better security and ease of use. For example, it’s harder for a computer to crack `FiremanSoccerPoodleLemon` than `FireSoc!22`, and also easier for a human to remember.

If using passwords for authentication, we do recommend adding multi-factor authentication as a requirement or option, for added security.

## Options for resetting a password

There are several ways to reset a user’s password.

### The user can select ‘forgot password’

Users can trigger a password reset by selecting ‘forgot password’ when they attempt to sign in. When they do this, they will be sent a one-time passcode via email. When they enter the code, they will be prompted to reset their password.

As an admin, you can also force a reset on behalf of the user — see [Reset a user’s password](/manage-users/access-control/reset-user-password/).

### Force a password reset via Kinde or API

You can trigger a password reset via the Kinde admin or via API. This is only suitable if you have an email for the user, as they will be sent a one-time passcode when they try to sign in next. They must enter the code to reset their password.

See [Reset a user’s password](/manage-users/access-control/reset-user-password/).

### Issue a temporary password

You can set a single-use password for new or existing users via the Kinde admin or via the Kinde Management API. Once you set the password, you need to communicate it to the user via your own chosen method - it cannot be sent from Kinde.

The user enters the temporary password to sign up or sign in, then they set their own password.

See [Set a temporary password](/manage-users/access-control/set-temporary-password/).

## Let users sign in with a code instead of a password

You can give users an alternative to entering their password. When this option is switched on, an **Or / Sign in with a code** button appears below the password field. If a user selects it, Kinde emails them a one-time passcode and they sign in by entering the code instead of their password.

This is useful when a user has forgotten their password but does not want to reset it, or when you want to reduce password friction without switching your app to passwordless entirely.

The user's password is not changed or removed. They can continue to sign in with it the next time.

### Switch on 'Sign in with a code'

The option is set on the authentication connection, so it applies to every app that uses that connection.

1. In Kinde, go to **Settings > Environment > Authentication**.
2. In the **Password** section, select **Configure** on the relevant tile: **Email + password** or **Username**. A configuration window opens.
3. In the **Settings** section, switch on **Show 'Sign in with a code' on password page**.

    ![sign in with a code toggle](https://imagedelivery.net/skPPZTHzSlcslvHjesZQcQ/9f096053-d047-4248-4e3f-c10993e2d000/socialsharingimage)
4. Select **Save**.

Repeat for the other password connection if you want the option on both.

<Aside type="warning">

The setting lives on the connection, not on the application. If several applications share the **Email + password** connection, switching this on affects all of them.

</Aside>

### Where the code is sent

| Sign-in identity | Where Kinde sends the code |
| --- | --- |
| Email + password | The email address the user entered to sign in |
| Username | The verified email address on the user's Kinde profile |

Kinde only ever sends the code to an email address that is already verified on the account. It never sends the code to an address supplied in the sign-in request, such as through `login_hint`.

The passcode behaves exactly like any other Kinde one-time passcode. It expires after 2 hours, and attack protection limits on sign-in attempts still apply. See [Passwordless authentication](/authenticate/authentication-methods/passwordless-authentication/) for more about how Kinde issues codes.

### When the option is not offered

The **Sign in with a code** button is hidden, and the code flow is refused, in these cases:

- **The user has a temporary password to enter.** They must use the temporary password and set their own password first. This applies even if an admin sets the temporary password after the sign-in page has already loaded.
- **The connection setting is off.** The option only appears for connections where you switched it on.

### Users without a verified email

A user needs a verified email address to receive a code. If they do not have one, Kinde still shows the code entry screen but does not send anything, so the user cannot complete sign-in this way and should use their password.

Kinde behaves this way on purpose. Showing the same screen regardless of whether an account exists or has a verified email means an attacker cannot use the button to work out which accounts are real. It is the same protection described under [attack protection](/build/set-up-options/attack-protection/). If a user reports that no code arrived, check that they have a verified email identity on their profile. See [Identity and verification](/authenticate/about-auth/identity-and-verification/).

### Multi-factor authentication still applies

Signing in with a code does not skip any other step in your sign-in flow. If the user is required to complete [multi-factor authentication](/authenticate/multi-factor-auth/about-multi-factor-authentication/), select an organization, or complete a required profile step, they are still prompted after entering the code.

### Customize the button and separator text

You can change the wording of both the separator and the button.

1. In Kinde, go to **Design > Page content**.
2. Select the language you want to edit.
3. Select the **Verify password** page.
4. Edit **Authentication method separator** (default: `Or`) and **Login with code button label** (default: `Sign in with a code`).
5. Select **Save**.

See [Update page content](/design/content-customization/update-auth-page-content/).

## Enable password authentication across apps

1. Go to **Settings > Environment > Authentication**.
2. In the **Password** section, select **Configure** on the relevant password tile: **Email + password** or **Username**. A configuration window opens.
3. Scroll to the bottom and switch password authentication on for the apps you want.
4. Select **Save**.

## Enable password authentication for a single app

1. Go to **Settings > Environment > Applications**.
2. Select **Configure** on the relevant application tile.
3. Select **Authentication** in the menu.
4. Switch on the password options you want.
5. Select **Save**.
